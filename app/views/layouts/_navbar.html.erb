<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-light bg-light text-center">
            <%= link_to "Events list", courses_path, :class => "nav-link" %>
            <%= link_to "Week view", week_path, :class => "nav-link" %>
            <%= link_to "Month view", month_path, :class => "nav-link" %>
            <%= link_to "Users", users_path, :class => "nav-link" if current_user && current_user.role == 'admin' %>
            
        </nav>
        <nav class="navbar navbar-expand-lg navbar-light bg-light text-center">
            <% if user_signed_in? %>
                <div> Welcome <%= current_user.email %></div>
                <div> &nbsp </div>
                <%= button_to "Sign out", destroy_user_session_path, method: :delete, :class => "btn btn-success" %>
            <% else %>
                <%= button_to "Sign in", new_user_session_path, :class => "btn btn-success" %>
            <% end %>
        </nav>
    </div>
</nav>

